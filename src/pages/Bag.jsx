import {Link} from 'react-router-dom';
import {Minus,Plus,Trash2,ArrowUpRight} from 'lucide-react';
import {useShop,money} from '../store/Shop';
export default function Bag(){const {items,quantity}=useShop();const total=items.reduce((n,i)=>n+i.price*i.quantity,0);return <div className="store-section store-shop"><span className="shop-kicker">YOUR NEXT EVERYDAY UPGRADE</span><h1>Your bag.</h1>{!items.length?<div className="store-empty"><p>Good things belong here.</p><Link className="store-pill" to="/products">Explore the collection <ArrowUpRight size={18}/></Link></div>:<div className="store-bag-layout"><div>{items.map(i=><article className="store-bag-item" key={i.id}><img src={i.image} alt={i.name}/><div><Link to={`/product/${i.id}`}>{i.name}</Link><p>{money(i.price)}</p><div className="store-quantity"><button aria-label={`Decrease ${i.name} quantity`} onClick={()=>quantity(i.id,i.quantity-1)}><Minus size={16}/></button><span>{i.quantity}</span><button aria-label={`Increase ${i.name} quantity`} onClick={()=>quantity(i.id,i.quantity+1)}><Plus size={16}/></button></div></div><button aria-label={`Remove ${i.name}`} onClick={()=>quantity(i.id,0)}><Trash2 size={19}/></button></article>)}</div><aside className="store-summary"><h2>Order summary</h2><div><span>Subtotal</span><strong>{money(total)}</strong></div><p>Shipping is calculated at checkout. Your order is checked against current pricing and stock.</p><Link className="store-pill" to="/checkout">Continue to checkout <ArrowUpRight size={17}/></Link></aside></div>}</div>}

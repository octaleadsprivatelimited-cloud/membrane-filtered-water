import {useState,useEffect} from 'react';
import {Link,NavLink,useLocation} from 'react-router-dom';
import {Droplets,Menu,X,ShoppingBag,Search,ArrowUpRight,UserRound} from 'lucide-react';
import {useShop} from '../store/Shop';
export default function Navbar(){const [open,setOpen]=useState(false);const location=useLocation();const {count}=useShop();useEffect(()=>setOpen(false),[location.pathname]);return <header className="store-header"><Link to="/" className="store-logo"><Droplets size={25}/><span>aqua<span>pure.</span></span></Link><nav className={open?'store-nav is-open':'store-nav'} aria-label="Main navigation">{[['/products','Shop all'],['/technology','The technology'],['/about','Our story'],['/services','Care & service']].map(([to,name])=><NavLink key={to} to={to}>{name}</NavLink>)}<Link to="/contact" className="store-nav-contact">Let’s talk <ArrowUpRight size={15}/></Link></nav><div className="store-nav-actions"><Link to="/account" aria-label="Your account"><UserRound size={20}/></Link><Link to="/products" aria-label="Search products"><Search size={20}/></Link><Link to="/bag" aria-label={`Shopping bag, ${count} items`}><ShoppingBag size={20}/><span>{count}</span></Link><button className="store-menu" aria-label={open?'Close menu':'Open menu'} aria-expanded={open} onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div></header>}

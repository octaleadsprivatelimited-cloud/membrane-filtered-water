rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All store writes and reads use the authenticated API. No direct client access.
    match /{document=**} { allow read, write: if false; }
  }
}
